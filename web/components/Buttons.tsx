import { Button } from '@mantine/core';

function Buttons() {
  return (
    <>
      <Button variant="gradient" gradient={{ from: 'orange', to: 'red' }}>Apply</Button>
    </>
  );
}
export default Buttons;
